# We will have more agents, that will be introduced as tools for the OG MC-Gemini.
# Every LLM needs to have its own config which contains the system prompt and the basic GenerateContentConfig values.
# Every Agent needs its own ToolRegistry.